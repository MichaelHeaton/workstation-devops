---
# Immutable entrypoint — customize vars/config.yml, not this file.
- name: Personal workstation setup
  hosts: localhost
  connection: local

  vars_files:
    - vars/config.yml

  roles:
    - role: directories
    - role: homebrew
    - role: repos
    - role: chezmoi
