---
shell_enabled: true

# Defaults for shell_personal.zsh.j2 (override in group_vars/personal.yml).
personal_aws_profile: platform-bootstrap
personal_aws_region: us-west-2
personal_tf_state_bucket: mccleaton-tfstate
# Starship env_var.VAULT_CLUSTER label when VAULT_ADDR is already set (homelab).
personal_vault_cluster: specterrealm
