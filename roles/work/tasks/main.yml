---
# Work profile marker — work-tagged tasks live in roles/common (shell, vault_tools, klam).
# Profile still controls repos/packages via group_vars/work.yml; feature tags control tooling.

- name: Mark work profile role applied
  ansible.builtin.set_fact:
    workstation_work_role_applied: true
  tags:
    - work
