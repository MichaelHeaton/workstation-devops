---
- name: Create Projects directory structure
  ansible.builtin.file:
    path: "{{ ansible_facts['env']['HOME'] }}/Projects/{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - adobe
    - personal
    - review
    - workspace
