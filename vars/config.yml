---
# DEPRECATED — configuration moved to group_vars/ (see group_vars/all.yml + work.yml | personal.yml).
# Profile is stored in ~/.workstation_profile (see profile_detect.yml / make profile).
# This file remains only so older docs/links fail loudly if referenced by custom tooling.

_deprecated: true
